/** Fallback daily goal when a summary has no explicit goal yet. */
export const DEFAULT_GOAL_ML = 2500;

/** A week is 7 days — used for windows, averages and adherence. */
export const DAYS_PER_WEEK = 7;
