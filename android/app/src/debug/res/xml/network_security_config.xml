<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network policy. The Firebase Local Emulator Suite serves over
  plain HTTP, which Android blocks by default. This permits cleartext traffic
  ONLY to the emulator hosts (the Android emulator reaches the host machine at
  10.0.2.2). This file lives in src/debug, so release builds keep cleartext
  fully disabled.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
