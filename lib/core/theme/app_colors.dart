import 'package:flutter/material.dart';

/// Centralized palette so surfaces, accents and states stay consistent.
abstract final class AppColors {
  static const background = Color(0xFF1E384D);
  static const surface = Color(0xFF274056);
  static const primary = Color(0xFF004984);
  static const accent = Color(0xFF4FA3E3);
  static const accentSoft = Color(0xFF7FC4F0);
  static const success = Color(0xFF34C759);
  static const danger = Color(0xFFC62828);
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white70;
  static const textMuted = Colors.white54;
}
